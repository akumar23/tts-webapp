[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[build.args]
# Use the CPU runtime stage
target = "runtime-cpu"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
